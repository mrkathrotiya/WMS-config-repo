server:
  port: 8081

cloud:
    gateway:
      discovery:
        locator:
          enabled: true  # Automatically route using service names
          lower-case-service-id: true

        # routes:
        #  - id: product-service
        #    uri: lb://product-service
        #   predicates:
        #    - Path=/products/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      register-with-eureka: true
      fetch-registry: true